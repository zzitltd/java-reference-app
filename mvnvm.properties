# Pins the Maven version for this project so every build uses the version the
# maven-enforcer-plugin requires (see <maven.version> in pom.xml). Without this,
# the mvnvm wrapper falls back to its built-in default (DEFAULT_MVN_VERSION, 3.9.9).
mvn_version=3.9.16
